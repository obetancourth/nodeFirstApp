var uploader = function(options){
  var defaults = {
    "formID" : "_defualtForm"
  }
  options = options || defualts;

  this.show = function(){
    htmlstr = '<form id="' + options.formID + '" methos="POST" >';
    htmlstr = '<form id="' + options.formID + '" methos="POST" >';
  }
}
